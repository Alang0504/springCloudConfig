---
spring:
  profiles: sit
  application:
    name: consumption-server-sit #服务名
    
server:
  port: 8090

eureka:
  client:
    service-url:
      defaultZone: http://peer1:8761/eureka/,http://peer2:8762/eureka/,http://peer3:8763/eureka/ #注册到服务中心
  instance:
    instance-id: test_sit
    prefer-ip-address: true

#开启feign的断路器
feign:
  hystrix:
    enabled: true